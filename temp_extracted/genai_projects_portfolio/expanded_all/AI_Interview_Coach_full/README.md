# AI Interview Coach — Full Starter (LLM)

**Overview**: An LLM-driven coach that generates interview questions for a role, records text answers, and provides structured feedback and a score using rubrics.

**Run locally**
```bash
python -m venv venv && source venv/bin/activate
pip install -r requirements.txt
streamlit run app.py
```
